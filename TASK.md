# ЗАДАНИЕ (L1, механика)

Создай модуль `temp_converter.py` с функциями:
- `c_to_f(c)` — Цельсий → Фаренгейт
- `f_to_c(f)` — Фаренгейт → Цельсий
- `c_to_k(c)` — Цельсий → Кельвин (при c < -273.15 выбросить ValueError)

И файл `test_temp_converter.py` с тестами через assert (не pytest-стиль,
обычный запуск python test_temp_converter.py): минимум 8 тестов,
включая граничные (-273.15, 0, 100) и проверку ValueError.

Критерий готовности: `python test_temp_converter.py` выводит, что все
тесты пройдены. Запреты: не создавай других файлов.
